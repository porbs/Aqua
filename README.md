# Aqua

To run server:
* npm install
* npm run start

To install Mocha:
* npm install -g mocha

To run continious testing:
* mocha tests --recursive  -watch

To run tests:
* npm install
* npm run test
